#include "main.h"

/**
 * mul - Multiplies two integer
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of mutiplication between x param y param
 */

int mul(int x, int y)
{
	return (x * y);
}
